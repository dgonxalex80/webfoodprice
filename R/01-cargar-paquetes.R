# 01- cargar paquetes
library(readxl)
library(rio) # leer y escribir datos en diferentes formatos
library(here) # gestionar rutas de archivos de manera sencilla
library(flexdashboard) # crear dashboards interactivos
library(shiny) # crear aplicaciones web interactivas
library(flextable)
library(plotly) # crear gráficos interactivos
library(readr) # leer datos de forma eficiente
library(tidyr) # manipulación de datos
library(sf) # trabajar con datos espaciales
library(rnaturalearth) # trabajar con datos geoespaciales
library(gridExtra) # crear gráficos múltiples
library(raster) # trabajar con datos raster
library(scales) # ajustar escalas en gráficos
library(tidyverse) # manipulación y visualización de datos
library(DT)

# ggplot2: crear gráficos elegantes y versátiles.
# tibble:  trabajar con data frames modernos (tibbles), que son una versión mejorada de los data frames de R base.
# tidyr: manipulación de datos en formato tidy, como la función gather() y spread().
# readr: lectura de datos de manera rápida y eficiente.
# purrr: forma más eficiente con listas y funciones.
# dplyr: manipulación de datos, con funciones como filter(), mutate(), group_by(), etc.
# stringr: manejo de cadenas de caracteres.
# forcats: manejo de factores.
# hms: manipular y trabajar con horas, minutos y segundos.
# lubridate: trabajar con fechas y horas de una manera más amigable.
# tidyselect: selección de columnas de manera más intuitiva.